# Docker

La configuracion principal esta en `docker-compose.yml` en la raiz del repositorio.

PostgreSQL queda accesible solo dentro de la red de Docker por defecto. La API espera a que la base de datos este saludable antes de iniciar.
